import { ComputedRef, Ref } from 'vue';
export declare const useInit: (countryCode: ComputedRef<string>, initialValue: string, international: boolean, silent: Ref<boolean>) => {
    initialData: any;
};
